# Base URL for the Flask REST API.
# Leave as the relative path in development — vite.config.js proxies /api to
# http://localhost:5000. Set an absolute URL for production builds.
VITE_API_BASE_URL=/api/v1
