import PagePlaceholder from '@/components/PagePlaceholder'

export default function ProposalWorkflow() {
  return (
    <PagePlaceholder
      title="Proposals"
      phase="11"
      description="The stage stepper and proposal list. The route is open to every role; the stage advance actions are role-gated inside the page."
    />
  )
}
