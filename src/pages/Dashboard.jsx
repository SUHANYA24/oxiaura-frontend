import PagePlaceholder from '@/components/PagePlaceholder'

export default function Dashboard() {
  return (
    <PagePlaceholder
      title="Dashboard"
      phase="6"
      description="Stat cards, the recent customers table, fraud alerts, the volume chart, and the AI engine status panel — all fed from reportService.dashboard()."
    />
  )
}
