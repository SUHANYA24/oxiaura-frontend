import PagePlaceholder from '@/components/PagePlaceholder'

export default function AgreementList() {
  return (
    <PagePlaceholder
      title="Agreements"
      phase="10"
      description="Agreement cards with the number in mono and the amount in the display serif, plus the generation form with the live maturity value."
    />
  )
}
