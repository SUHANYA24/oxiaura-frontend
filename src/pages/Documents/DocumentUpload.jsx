import PagePlaceholder from '@/components/PagePlaceholder'

export default function DocumentUpload() {
  return (
    <PagePlaceholder
      title="Upload document"
      phase="8"
      description="Dropzone with client-side validation and an upload progress bar, the stage-by-stage OCR and fraud pipeline indicator, then the extracted fields with confidence chips."
    />
  )
}
