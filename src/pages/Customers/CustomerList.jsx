import PagePlaceholder from '@/components/PagePlaceholder'

export default function CustomerList() {
  return (
    <PagePlaceholder
      title="Customers"
      phase="7"
      description="Debounced search across name, NIC, and code, a segmented status filter, and the paginated table with row click through to detail."
    />
  )
}
